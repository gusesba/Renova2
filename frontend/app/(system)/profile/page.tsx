import { ProfileDashboard } from "@/app/(system)/profile/components/profile-dashboard";

// Tela de manutencao do proprio usuario autenticado.
export default function ProfilePage() {
  return <ProfileDashboard />;
}
