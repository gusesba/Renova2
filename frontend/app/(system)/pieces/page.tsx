import { PiecesDashboard } from "@/app/(system)/pieces/components/pieces-dashboard";

// Rota principal do modulo 06 dentro do shell autenticado.
export default function PiecesPage() {
  return <PiecesDashboard />;
}
