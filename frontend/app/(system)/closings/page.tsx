import { ClosingsDashboard } from "@/app/(system)/closings/components/closings-dashboard";

// Expone a rota visual do modulo 13 dentro do shell autenticado.
export default function ClosingsPage() {
  return <ClosingsDashboard />;
}
