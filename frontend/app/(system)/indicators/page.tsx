import { IndicatorsDashboard } from "@/app/(system)/indicators/components/indicators-dashboard";

// Expone a rota visual do modulo 14 dentro do shell autenticado.
export default function IndicatorsPage() {
  return <IndicatorsDashboard />;
}
