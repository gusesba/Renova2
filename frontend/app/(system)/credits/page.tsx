import { CreditsDashboard } from "@/app/(system)/credits/components/credits-dashboard";

// Expone a rota visual do modulo 10 dentro do shell autenticado.
export default function CreditsPage() {
  return <CreditsDashboard />;
}
