import { ReportsDashboard } from "@/app/(system)/reports/components/reports-dashboard";

// Expoe a rota visual do modulo 15 dentro do shell autenticado.
export default function ReportsPage() {
  return <ReportsDashboard />;
}
