import { DocumentsDashboard } from "@/app/(system)/documents/components/documents-dashboard";

// Expoe a rota visual do modulo 16 dentro do shell autenticado.
export default function DocumentsPage() {
  return <DocumentsDashboard />;
}
