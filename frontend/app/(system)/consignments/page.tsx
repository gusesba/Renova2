import { ConsignmentsDashboard } from "@/app/(system)/consignments/components/consignments-dashboard";

// Renderiza a pagina principal do modulo 07 dentro do shell autenticado.
export default function ConsignmentsPage() {
  return <ConsignmentsDashboard />;
}
