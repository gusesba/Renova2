import { LoginScreen } from "@/app/login/components/login-screen";

// Pagina isolada de autenticacao, fora do shell principal do sistema.
export default function LoginPage() {
  return <LoginScreen />;
}
